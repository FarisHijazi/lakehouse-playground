# Core
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
# faker>=20.0.0  # No longer needed — using real NYC TLC data

# Data Warehouse (local)
duckdb>=0.9.0

# dbt
dbt-core>=1.7.0
dbt-duckdb>=1.7.0

# Spark
pyspark>=3.5.0
delta-spark>=3.0.0

# Data Quality
great-expectations>=0.18.0

# Airflow (installed via Docker, but useful locally too)
apache-airflow>=2.7.0

# Visualization
plotly>=5.18.0
matplotlib>=3.8.0

# Utilities
python-dateutil>=2.8.0
requests>=2.31.0
pyyaml>=6.0.0
Jinja2>=3.1.0
